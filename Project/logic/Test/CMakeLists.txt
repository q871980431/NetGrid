FILE(GLOB ROOT_DIRS "${PROJECT_SOURCE_DIR}/logic/Test/*")
FOREACH(SUB_DIR ${ROOT_DIRS})
	MESSAGE("添加子目录:${SUB_DIR}")
	string( COMPARE NOTEQUAL ${SUB_DIR} "${PROJECT_SOURCE_DIR}/logic/Test/CMakeLists.txt" NOT_CMAKELISET)
	IF(NOT_CMAKELISET)
		ADD_SUBDIRECTORY(${SUB_DIR})
	ENDIF()
ENDFOREACH()
