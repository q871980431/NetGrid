设计一个对象,用于进行KV存储
	Key为s32类型
	
需求:
	基本元素的KV存储
	数组支持
	有序集合支持
	自身递归
	

设计一个数据容器对象

访问接口
	采用统一的 只读访问凭据进行访问, 诸如attr_def::name
需求,
	基本元素访问,
	数组支持
	有序集合支持?
	自身组合

	是否支持序列化?
容器内存布局,仅支持单根布局	子类属性不能与基类属性同名,确保 同一个名字在类族里面具有唯一性
OBJ

Base
POD1
POD2
OBJ

以便于在不同的模块和语言层,实现统一的访问形式,并且在各自语言层均可以有较高的速度进行访问
